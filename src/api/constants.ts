const Host = "ya-praktikum.tech";

const BaseURL = `${Host}/api/v2`;
const Protocol = "https://";

const WebSocketURL = `${Host}/ws`;
const ProtocolWS = "wss://";


export {
  BaseURL, Protocol,
  WebSocketURL, ProtocolWS
};
